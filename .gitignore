# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (root holds only vitest for the shared tests; mobile has its own)
node_modules/

# testing
/coverage

# build output / caches
*.tsbuildinfo
/.next/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# editor / local
.claude/settings.local.json

# leftover local data from the old desktop build (never committed)
/data/app.db
/data/app.db-*
/data/images/
/runtime/
