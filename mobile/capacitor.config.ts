import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.phatbanana.ygodeckbuilder",
  appName: "YGO Deck Builder",
  webDir: "dist",
};

export default config;
