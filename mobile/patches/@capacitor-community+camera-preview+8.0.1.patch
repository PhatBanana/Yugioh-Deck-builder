diff --git a/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraPreview.java b/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraPreview.java
index 854aeee..1682eb6 100644
--- a/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraPreview.java
+++ b/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraPreview.java
@@ -68,6 +68,107 @@ public class CameraPreview extends Plugin implements CameraActivity.CameraPrevie
         }
     }
 
+    @PluginMethod
+    public void setZoom(PluginCall call) {
+        if (this.hasCamera(call) == false) {
+            call.reject("Camera is not running");
+            return;
+        }
+        try {
+            Integer level = call.getInt("level", 0);
+            Camera camera = fragment.getCamera();
+            Camera.Parameters params = camera.getParameters();
+            if (!params.isZoomSupported()) {
+                call.reject("Zoom not supported on this camera");
+                return;
+            }
+            int clamped = Math.max(0, Math.min(params.getMaxZoom(), level));
+            params.setZoom(clamped);
+            fragment.setCameraParameters(params);
+            call.resolve();
+        } catch (Exception e) {
+            Logger.debug(getLogTag(), "setZoom exception: " + e);
+            call.reject("failed to set zoom");
+        }
+    }
+
+    @PluginMethod
+    public void getZoomState(PluginCall call) {
+        if (this.hasCamera(call) == false) {
+            call.reject("Camera is not running");
+            return;
+        }
+        try {
+            Camera camera = fragment.getCamera();
+            Camera.Parameters params = camera.getParameters();
+            boolean supported = params.isZoomSupported();
+            JSObject state = new JSObject();
+            state.put("supported", supported);
+            state.put("max", supported ? params.getMaxZoom() : 0);
+            state.put("current", supported ? params.getZoom() : 0);
+            call.resolve(state);
+        } catch (Exception e) {
+            Logger.debug(getLogTag(), "getZoomState exception: " + e);
+            call.reject("failed to read zoom state");
+        }
+    }
+
+    @PluginMethod
+    public void refocus(PluginCall call) {
+        if (this.hasCamera(call) == false) {
+            call.reject("Camera is not running");
+            return;
+        }
+        try {
+            final Camera camera = fragment.getCamera();
+            camera.cancelAutoFocus();
+            camera.autoFocus(
+                new Camera.AutoFocusCallback() {
+                    @Override
+                    public void onAutoFocus(boolean success, Camera c) {
+                        // one-shot refocus; nothing to report back
+                    }
+                }
+            );
+            call.resolve();
+        } catch (Exception e) {
+            Logger.debug(getLogTag(), "refocus exception: " + e);
+            call.reject("failed to refocus");
+        }
+    }
+
+    @PluginMethod
+    public void setFocusMode(PluginCall call) {
+        if (this.hasCamera(call) == false) {
+            call.reject("Camera is not running");
+            return;
+        }
+        try {
+            String mode = call.getString("mode", "auto");
+            Camera camera = fragment.getCamera();
+            Camera.Parameters params = camera.getParameters();
+            java.util.List<String> supported = params.getSupportedFocusModes();
+            if (supported == null) { call.resolve(); return; }
+            String target;
+            if ("macro".equals(mode) && supported.contains(Camera.Parameters.FOCUS_MODE_MACRO)) {
+                target = Camera.Parameters.FOCUS_MODE_MACRO;
+            } else if (supported.contains(Camera.Parameters.FOCUS_MODE_CONTINUOUS_PICTURE)) {
+                target = Camera.Parameters.FOCUS_MODE_CONTINUOUS_PICTURE;
+            } else if (supported.contains(Camera.Parameters.FOCUS_MODE_AUTO)) {
+                target = Camera.Parameters.FOCUS_MODE_AUTO;
+            } else {
+                call.resolve();
+                return;
+            }
+            params.setFocusMode(target);
+            fragment.setCameraParameters(params);
+            call.resolve();
+        } catch (Exception e) {
+            Logger.debug(getLogTag(), "setFocusMode exception: " + e);
+            call.reject("failed to set focus mode");
+        }
+    }
+
     @PluginMethod
     public void setOpacity(PluginCall call) {
         if (this.hasCamera(call) == false) {
