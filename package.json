{
  "name": "ygo-deck-builder-shared-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for the shared core logic used by the mobile app (mobile/).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
